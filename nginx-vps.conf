# Example Nginx config for VPS — place in /etc/nginx/sites-available/doc-manager
# Replace doc-manager.example.com with your actual domain.

server {
    listen 80;
    server_name doc-manager.example.com;

    # Redirect HTTP to HTTPS (uncomment after certbot setup)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS block (uncomment and configure after certbot setup)
# server {
#     listen 443 ssl;
#     server_name doc-manager.example.com;
#
#     ssl_certificate     /etc/letsencrypt/live/doc-manager.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/doc-manager.example.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:80;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
